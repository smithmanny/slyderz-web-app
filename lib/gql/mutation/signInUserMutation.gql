mutation signInUser(
  $identifier: String!
  $password: String!
) {
  signInUser(
    input: {
      data: { identifier: $identifier, password: $password }
    }
  ) {
    user {
      username
      email
      firstName
      lastName
    }
  }
}
