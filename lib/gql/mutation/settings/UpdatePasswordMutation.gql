mutation UpdatePassword($password: String!, $newPassword: String!, $confirmNewPassword: String!) {
  updatePassword(password: $password!, newPassword: $newPassword, confirmNewPassword: $confirmNewPassword) {
    id
  }
}